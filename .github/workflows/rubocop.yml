name: RuboCop

on: [pull_request]

jobs:
  rubocop:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v7
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "4.0"
          bundler-cache: true
      - run: bundle exec rubocop
