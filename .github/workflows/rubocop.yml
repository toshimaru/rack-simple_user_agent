name: RuboCop

on: [pull_request]

jobs:
  rubocop:
    runs-on: ubuntu-latest
    name: rubocop
    steps:
    - uses: actions/checkout@v2
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 2.6
        bundler-cache: true
    - run: bundle exec rubocop
